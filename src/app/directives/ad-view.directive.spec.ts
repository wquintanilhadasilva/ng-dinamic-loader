import { AdViewDirective } from './ad-view.directive';

describe('AdViewDirective', () => {
  it('should create an instance', () => {
    const directive = new AdViewDirective();
    expect(directive).toBeTruthy();
  });
});
